(function () {
    'use strict';

    angular
        .module('app')
        .controller('HomeController', ['$location', 'FlashService',
            function ($location, FlashService) {
                var vm = this;

                (function initController() {
                })();
            }
        ]);

})();
